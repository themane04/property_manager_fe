export interface RentalUnit {
    id: string
    designation: string
    type: string
    area_m2: string
    number_of_rooms: string
    rent: string
    available_from: string
    status: string
    tenant: string
    properties: string
    features: string[]
}
